---
- name: Site setup for bonesdeploy (Laravel)
  hosts: all
  become: true
  vars:
    laravel_php_fpm_socket_path: "/run/{{ project_name }}/php-fpm.sock"

  roles:
    - role: users
      tags: [users]

    - role: apparmor
      tags: [apparmor]

    - role: common
      tags: [common]

    - role: laravel_runtime
      tags: [laravel]

    - role: nginx
      tags: [nginx]

    - role: ssl
      tags: [ssl]

  post_tasks:
    - name: Display setup completion
      ansible.builtin.debug:
        msg: |
          Bones Laravel site setup completed.
          Deploy user: {{ deploy_user }}
          Service user: {{ service_user }}
          Group: {{ group }}
      tags: always
