---
- name: Install Rails runtime packages
  ansible.builtin.apt:
    name: "{{ rails_runtime_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [rails, packages]
