---
- name: Site setup for bonesdeploy (Vue)
  hosts: all
  become: true

  roles:
    - role: users
      tags: [users]

    - role: apparmor
      tags: [apparmor]

    - role: common
      tags: [common]

    - role: vue_runtime
      tags: [vue]

    - role: nginx
      tags: [nginx]

    - role: ssl
      tags: [ssl]

  post_tasks:
    - name: Display setup completion
      ansible.builtin.debug:
        msg: |
          Bones Vue site setup completed.
          Deploy user: {{ deploy_user }}
          Service user: {{ service_user }}
          Group: {{ group }}
      tags: always
