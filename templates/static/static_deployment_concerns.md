```bash
#!/usr/bin/env bash

set -Eeuo pipefail

# Static site — no build step required.
# Files are served directly after checkout.
#
# If you need a build step (e.g., a static site generator),
# add your commands here.

echo "Static deployment complete. No build step configured."
```
