server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    root {{ nginx_placeholder_root }};
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
