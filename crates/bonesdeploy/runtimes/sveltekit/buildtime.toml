# ── Build-time environment variables ──────────────────────
# Declare which env var names from shared/.env should be
# injected into the build container.
#
# SvelteKit inlines PUBLIC_* vars (via $env/static/public)
# into the client bundle at build time, so any PUBLIC_* var
# your app uses must be listed here.
#
# Values come from `bonesdeploy secrets push` —
# do not put values here.
#
vars = [
  # "PUBLIC_API_URL",
  # "PUBLIC_SITE_NAME",
]
