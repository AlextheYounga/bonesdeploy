# Declare which env var names from shared/.env should be injected into
# the build container. Vite inlines VITE_* vars into the client bundle
# at build time, so they must be available during `vite build`.
#
# Values come from `bonesdeploy secrets push` — do not put values here.
#
vars = [
  # "VITE_API_URL",
  # "VITE_APP_TITLE",
]
