#!/usr/bin/env bash
set -euo pipefail

ENV_PATH="{{ env_path }}"

if [ ! -s "$ENV_PATH" ]; then
    printf '%s' {{ config_content }} > "$ENV_PATH"
fi
chown root:root "$ENV_PATH"
chmod 600 "$ENV_PATH"
